{
  "name": "h5a-adapter",
  "version": "0.1.0",
  "scripts": {
    "dist": "terser --mangle --compress --comments false --timings --output ./adapter.min.js ./index.js && gsed -i '1s/^/\\/* h5a-adapter 0.1.0 *\\/\\n/' ./adapter.min.js"
  },
  "devDependencies": {
    "terser": "5.5.1"
  }
}
